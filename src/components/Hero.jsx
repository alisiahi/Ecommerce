import ActiveSlider from "./ActiveSlider";

const Hero = () => {
  return (
    <section className="container mx-auto py-20 border-b">
      <ActiveSlider />
    </section>
  );
};

export default Hero;
